name: exam_project
description: Grading template
publish_to: none
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Package được CHO PHÉP dùng trong testcase (vd DateFormat). Thêm package ở đây + build lại
  # ảnh nền là testcase được phép import; backend tự đọc danh sách này để validate lúc upload đề.
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true