# ── Java / Maven ────────────────────────────────────────────────
grader/target/
*.class
*.jar
!**/maven-wrapper.jar

# ── Node / Next.js ──────────────────────────────────────────────
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/.turbo/
npm-debug.log*
yarn-error.log*

# ── Dữ liệu runtime của hệ thống chấm ───────────────────────────
# exams/ chứa testcase đã upload (mount lúc chấm) — dữ liệu runtime
exams/
# submissions/ lưu zip bài nộp SV để audit — dữ liệu runtime
submissions/

# ── IDE / OS ────────────────────────────────────────────────────
.idea/
*.iml
.vscode/
.DS_Store
Thumbs.db

# ── Logs / tạm ──────────────────────────────────────────────────
*.log
*.tmp
# Thư mục output thử nghiệm của bộ sinh đề AI (cục bộ)
grader/ai-test-output/
grader/ai-zip-test/

# ── Tài liệu cục bộ (không commit) ──────────────────────────────
docs/context-diagram.md
docs/context-diagram.drawio

# ── BÍ MẬT: API key AI (TUYỆT ĐỐI không commit) ─────────────────
# Key nằm ở secret.properties (Spring tự nạp qua spring.config.import).
# File .example là TEMPLATE (được commit) — KHÔNG ignore.
secret.properties
**/secret.properties
**/application-local.properties
**/application-secret.properties
**/config/application.properties
.env
.env.local
