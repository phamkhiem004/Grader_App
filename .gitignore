# ── Java / Maven ────────────────────────────────────────────────
grader/target/
*.class
*.jar
!**/maven-wrapper.jar

# ── Node / Next.js ──────────────────────────────────────────────
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/.turbo/
npm-debug.log*
yarn-error.log*

# ── Dữ liệu runtime của hệ thống chấm ───────────────────────────
# Neo vào gốc repo: fixtures/ cũng có thư mục tên submissions/ nhưng là dữ liệu
# nghiệm thu phải commit, không phải dữ liệu runtime.
# exams/ chứa testcase đã upload (mount lúc chấm) — dữ liệu runtime
/exams/
# submissions/ lưu zip bài nộp SV để audit — dữ liệu runtime
/submissions/
# Thư mục tạm do fixtures/result-json-v2/run-fixture.sh sinh ra
fixtures/*/.build/

# ── IDE / OS ────────────────────────────────────────────────────
.idea/
*.iml
.vscode/
.DS_Store
Thumbs.db

# ── Local AI/coding-agent config ─────────────────────────────────
# Thường chứa quyền chạy lệnh, đường dẫn máy cá nhân, hoặc ghi chú cục bộ.
.claude/
.codex/
.agents/

# ── Logs / tạm ──────────────────────────────────────────────────
*.log
*.tmp
_tmp*/
_codex_work/
.codex_scratch/
.pytest_cache/
__pycache__/
# Thư mục output thử nghiệm của bộ sinh đề AI (cục bộ)
grader/ai-test-output/
grader/ai-zip-test/

# ── Tài liệu cục bộ (không commit) ──────────────────────────────
docs/context-diagram.md
docs/context-diagram.drawio

# ── BÍ MẬT: API key AI (TUYỆT ĐỐI không commit) ─────────────────
# Key nằm ở secret.properties (Spring tự nạp qua spring.config.import).
# File .example là TEMPLATE (được commit) — KHÔNG ignore.
secret.properties
**/secret.properties
**/application-local.properties
**/application-secret.properties
**/config/application.properties
.env
.env.local
.env.*
**/.env
**/.env.*
!.env.example
!**/.env.example

# Chứng chỉ/private key cục bộ
*.pem
*.key
*.p12
*.pfx
*.jks

# Database/cache cục bộ
*.db
*.sqlite
*.sqlite3
mysql/data/
mysql_data/
