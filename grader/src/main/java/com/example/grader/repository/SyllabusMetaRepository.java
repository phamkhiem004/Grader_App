package com.example.grader.repository;

import com.example.grader.entity.SyllabusMeta;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SyllabusMetaRepository extends JpaRepository<SyllabusMeta, Long> {
}
