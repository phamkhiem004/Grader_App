/**
 * Compatibility route for old bookmarks.
 *
 * Testcase authoring now uses the Golden Solution Record–Abstract–Replay
 * workflow. The previous drag/drop Dart testcase builder was intentionally
 * removed so there is only one source of truth for new suites.
 */
export { default } from "../behavior-authoring/page";
